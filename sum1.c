#include <stdio.h>

int main()
{
    int a = 5;       // Declare and assign a
    int b = 10;      // Declare and assign b
    int sum = a + b; // Calculate sum

    printf("%d", a);
    printf("%d", b);
    printf("The sum of %d and %d is %d\n", a, b, sum);

    return 0;
}
